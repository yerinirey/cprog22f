#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "todo.h"

/* 날짜 및 중요도를 체크해서 할 일을 관리하는 프로그램 */
int main() {
    head = NULL;
    int num;
    count = 0;      //리스트 수 초기화
    while(1) {
        SelectMenu(num);
    }
}